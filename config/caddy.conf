{
  # Global rate limits (none for baseline)
}

http://localhost:8081 {
  encode gzip
  reverse_proxy http://localhost:3000

  # Disable caching
  header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0"
  header Pragma "no-cache"
  header Expires "0"
}
